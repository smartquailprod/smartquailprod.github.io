apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sqdovecot100.fullname" . }}-test-connection"
  labels:
    {{- include "sqdovecot100.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sqdovecot100.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
