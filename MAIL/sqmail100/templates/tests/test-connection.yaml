apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sqmail100.fullname" . }}-test-connection"
  labels:
    {{- include "sqmail100.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sqmail100.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
