apiVersion: v1
kind: Service
metadata:
  name: smartquaildb17
  namespace: db-srv
  labels:
    app: postgres
spec:
  selector:
    app: postgres
  ports:
  - protocol: TCP
    port: 5432
    targetPort: 5932
  type: ClusterIP