apiVersion: v1
kind: Service
metadata:
  name: {{ include "sqdovecot100.fullname" . }}
  labels:
    {{- include "sqdovecot100.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sqdovecot100.selectorLabels" . | nindent 4 }}
